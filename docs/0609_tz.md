надо теперь  тебе самому собрать контейнер под serverless данного проекта чтобы все  работало как надо, показать пример ответа, но я хочу чтобы ответы возвращались вот в   таком виде вот отрезок изх другого проекта handler.py
```
return {
            "images_base64": [base64.b64encode(buf.getvalue()).decode("utf-8")],
            "time": round(time.time() - t0, 2),
            "steps": parsed.steps,
            "seed": seed,
        }
```
все собирай в сервере данные которого лежат в .env SERVER_IP и тд как все запушаешь в докер докенр логин уже есть, краоч сделаем прям все грамотно и как надо пожалуйста
